## .ENV File

Before Run the Project need to add .env file on root dir.

### The env file must have those fild:
* PORT => an Port 
* MONGO_URI => mongo connection string
* SECRATE => secrate text
* CLIENT_URL => frontend url